@using CompnayWebsite_Models.Models
@model List<Company>

    <div class="container p-3">
    <div class="row pt-4">
        <div class="col-6">
            <h2 class="text-primary">List of Companies</h2>
        </div>
        <div class="col-6 text-right">
            <a asp-controller="Company" asp-action="Create" class="btn btn-primary">
                Add New Company
            </a>
            <a asp-controller="Excel" asp-action="ExportExcelFile" class="btn btn-success ml-2">
                Export to Excel
            </a>
        </div>
    </div>
</div>

@*@if (Model.Count > 0)
{
    

        <ejs-grid id="companies" dataSource="@Model" gridLines="None" allowPaging="true" allowSorting="true" >
            <e-grid-pagesettings pageSize="10" ></e-grid-pagesettings>
            <e-grid-columns >
                <e-grid-column headerText="ID" field="CompanyID"></e-grid-column>
                <e-grid-column headerText="Name" field="CompanyName"></e-grid-column>
                <e-grid-column headerText="Phone" field="PhoneNumber"></e-grid-column>
            <e-grid-column headerText="Email" field="Email"></e-grid-column>
            <e-grid-column headerText="" template="#TypeofColumnTemplate" ></e-grid-column>
                
            </e-grid-columns>

        </ejs-grid>
       
 @foreach (var company in Model)
    {

        <script id="TypeofColumnTemplate" type="text/x-template">
            <div class="d-flex flex-row justify-content-lg-start">
                <a class="btn btn-sm text-decoration-none" href="/Company/Edit/{CompanyID}">
                    <i class="fas fa-pen"></i>
                </a>
            </div>
        </script>

    }   
 
}
else
    {
        <p>No company exists.</p>
    }
*@

@if (Model.Count > 0)
{
    <div class="container">
        <table class="table">
            <thead class="thead-dark">
                <tr>
                    <th scope="col">Company ID</th>
                    <th scope="col">Company Name</th>
                    <th scope="col">Phone Number</th>
                    <th scope="col">Email</th>
                    <th scope="col"></th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>@item.CompanyID</td>
                        <td>@item.CompanyName</td>
                        <td>@item.PhoneNumber</td>
                        <td>@item.Email</td>
                        <td>
                            <div class="btn-group" role="group">
                                <a asp-controller="Company" asp-action="Edit" asp-route-id="@item.CompanyID" class="btn btn-primary">
                                    <i class="fas fa-pen"></i>
                                </a>
                            </div>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
}
else
{
    <p>No company exists.</p>
}

